#pragma once
#include <string>
#include <fstream>

/**
 * @class DoublyLinkedList
 * @brief 指定したファイルを取り込める双方向リスト
 *
 * txtファイルを指定し、読み込むことで、
 * 再度同順に出力できます
 */
template<class T>
class DoublyLinkedList {
public:
	/**
	 * @brief ファイル読み込み
	 * @param ファイルパス
	 */
	void inputData(std::string txtName) {

	}

	/**
	 * @brief 読み込んだ内容の出力
	 * @return 文字列(string)
	 */
	std::string outputData() {

	}
private:

};